# Contract fixtures

These are synthetic schema/loader test inputs, not generated training courses, physical trajectories or released model manifests. Zero-filled hashes deliberately test syntax only. They must never be published as benchmark or model evidence. Geometric course validation belongs to M2; these tests cover structural contracts and cross-field invariants.
